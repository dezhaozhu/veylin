/** Default agent package id (examples/veylin). */
export const DEFAULT_AGENT_ID = 'veylin';

/** Default catalog model id when agent.yaml uses `model: default`. */
export const DEFAULT_MODEL_CATALOG_ID = 'default';

/** Generic status options for new status-type table columns. */
export const DEFAULT_TABLE_STATUS_OPTIONS = ['open', 'in_progress', 'done'] as const;
